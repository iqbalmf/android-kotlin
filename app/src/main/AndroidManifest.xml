<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.iqbalfauzan.mykotlinapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".submission_dua.Main2Activity" />
        <activity android:name=".submission_dua.details.DetailActivity" />
        <activity android:name=".submission_akhir.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".submission_akhir.TeamActivity" />
        <activity android:name=".submission_akhir.DetailMatchActivity" />
        <activity android:name=".submission_akhir.PlayerDetailActivity"></activity>
    </application>

</manifest>